html, body {
    margin: 0;
    padding: 0;
    height: 100%;
    overflow: hidden;
    background-color: #0d0d1a; 
    background-image: url('bg.png'); 
    background-size: cover;          
    background-position: center;     
    background-repeat: no-repeat;    
    color: #eaeaea; 
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
    display: flex; 
    flex-direction: column; 
    align-items: center;
    justify-content: center;
}

.container {
    width: 100%;
    height: 98vh;
    max-width: 1100px; 
    display: flex;
    flex-direction: column;
    padding: 15px;
    box-sizing: border-box;
    position: relative;
    transition: transform 0.1s;
}

h1 { margin: 0 0 10px 0; color: #e94560; text-align: center; font-size: 1.8rem; letter-spacing: 2px; text-shadow: 0 0 10px rgba(233, 69, 96, 0.4); }

/* ÚJ: LOBBY (VÁRÓTEREM) STÍLUSOK */
#lobby-screen {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: fadeIn 0.3s ease-in-out;
}

.lobby-box {
    background: linear-gradient(135deg, rgba(22, 33, 62, 0.95) 0%, rgba(16, 16, 36, 0.95) 100%);
    border: 3px solid #0f3460;
    border-radius: 16px;
    padding: 30px;
    width: 100%;
    max-width: 450px;
    box-shadow: 0 15px 35px rgba(0,0,0,0.7);
    text-align: center;
}

.lobby-box h2 {
    margin-top: 0;
    margin-bottom: 25px;
    color: #fff;
    font-size: 1.4rem;
    border-bottom: 2px solid #0f3460;
    padding-bottom: 10px;
}

.lobby-section {
    margin: 20px 0;
}

.lobby-section p {
    margin: 0 0 8px 0;
    font-size: 0.95rem;
    color: #ccc;
}

.code-display {
    background-color: rgba(5, 5, 10, 0.8);
    border: 1px dashed #f39c12;
    padding: 12px;
    font-family: 'Courier New', Courier, monospace;
    font-size: 1.1rem;
    font-weight: bold;
    color: #f39c12;
    border-radius: 6px;
    letter-spacing: 1px;
    user-select: all; /* Egy kattintásra kijelöli az egészet a másoláshoz */
    box-shadow: inset 0 0 10px rgba(0,0,0,0.5);
}

.hint {
    font-size: 0.75rem !important;
    color: #888 !important;
    margin-top: 5px !important;
}

.divider {
    margin: 25px 0;
    font-size: 0.8rem;
    color: #555;
    font-weight: bold;
    position: relative;
}

#opponent-id-input {
    width: 100%;
    padding: 12px;
    background-color: rgba(5, 5, 10, 0.8);
    border: 2px solid #0f3460;
    border-radius: 6px;
    color: #fff;
    font-size: 1rem;
    box-sizing: border-box;
    text-align: center;
    margin-bottom: 12px;
    transition: border-color 0.2s;
}

#opponent-id-input:focus {
    outline: none;
    border-color: #e94560;
}

#connect-btn {
    width: 100%;
    padding: 12px;
    background-color: #e94560;
    color: white;
    border: none;
    border-radius: 6px;
    font-size: 1rem;
    font-weight: bold;
    cursor: pointer;
    box-shadow: 0 4px 10px rgba(233, 69, 96, 0.3);
    transition: background-color 0.2s;
}

#connect-btn:hover { background-color: #ff4757; }

#connection-status {
    margin-top: 20px;
    font-size: 0.85rem;
    color: #39a2db;
    font-style: italic;
}

/* JÁTÉKTÉR JELZŐK */
#turn-indicator {
    font-size: 1.1rem;
    font-weight: bold;
    margin: 0 auto 15px auto;
    padding: 6px 25px;
    border-radius: 20px;
    background-color: #0f3460;
    border: 2px solid #e94560;
    text-align: center;
    width: fit-content;
    box-shadow: 0 4px 10px rgba(0,0,0,0.3);
}

.arena { 
    display: flex; 
    flex-direction: row; 
    justify-content: center; 
    gap: 50px; 
    flex: 1; 
    min-height: 0; 
    align-items: center;
}

/* VERTIKÁLIS KÁRTYA FORMÁTUM */
.card { 
    border: 3px solid #0f3460; 
    border-radius: 16px; 
    padding: 15px; 
    background: linear-gradient(135deg, rgba(22, 33, 62, 0.95) 0%, rgba(26, 26, 46, 0.95) 100%);
    display: flex;
    flex-direction: column;
    width: 330px; 
    height: 560px; 
    box-sizing: border-box;
    transition: transform 0.2s ease, border-color 0.2s, box-shadow 0.2s, filter 0.3s;
    position: relative;
    z-index: 1;
    box-shadow: 0 10px 25px rgba(0,0,0,0.6);
}

.card.active-card { 
    border-color: #e94560; 
    box-shadow: 0 0 20px rgba(233, 69, 96, 0.6);
    transform: translateY(-5px);
}

.card-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
.card h2 { margin: 0; font-size: 1.2rem; color: #fff; font-weight: bold; text-shadow: 0 2px 4px rgba(0,0,0,0.5); }
.card-stats { text-align: right; }

.hp-bar { width: 100%; height: 12px; background-color: #222; border-radius: 6px; margin-bottom: 12px; overflow: hidden; border: 1px solid #444; }
.hp-fill { height: 100%; background-color: #2ecc71; transition: width 0.4s ease-in-out, background-color 0.4s; }

.card-body { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; position: relative; margin-bottom: 15px; min-height: 0; }
.card-image { width: 100%; height: 100%; max-height: 230px; object-fit: cover; border-radius: 10px; border: 2px solid #0f3460; box-shadow: inset 0 0 20px rgba(0,0,0,0.6); }

.status-container { display: flex; gap: 6px; justify-content: center; margin-top: 8px; position: absolute; bottom: 5px; }
.status-badge { color: #000; font-weight: bold; font-size: 0.7rem; padding: 3px 8px; border-radius: 5px; box-shadow: 0 2px 5px rgba(0,0,0,0.3); }
.badge-para { background-color: #f1c40f; }
.badge-burn { background-color: #e74c3c; color: white; }
.badge-shield { background-color: #3498db; color: white; }

.stat-text { margin: 1px 0; font-size: 0.85rem; font-weight: bold; color: #ccc; }
.ap-count { color: #4db8ff; font-size: 0.95rem; }

/* KÉPESSÉG RÁCS */
.attacks { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
.attacks button { padding: 10px 5px; background-color: #0f3460; color: white; border: 1px solid #16213e; border-radius: 6px; cursor: pointer; font-weight: bold; font-size: 0.75rem; display: flex; flex-direction: column; align-items: center; justify-content: center; line-height: 1.3; transition: background-color 0.2s, transform 0.1s; }
.attacks button:hover:not(:disabled) { background-color: #1a4f8a; transform: translateY(-1px); }
.attacks button:active:not(:disabled) { transform: translateY(1px); }
.attacks button span { font-size: 0.6rem; color: #39a2db; margin-top: 2px; font-weight: normal; }
.attacks button:disabled { background-color: #222533; color: #555; border-color: #222; cursor: not-allowed; }

.attacks .charge-btn { grid-column: span 2; background: linear-gradient(90deg, #d35400 0%, #f39c12 100%); color: #fff; padding: 10px; font-size: 0.8rem; border: none; text-shadow: 0 1px 2px rgba(0,0,0,0.5); }
.attacks .charge-btn span { color: #fff; opacity: 0.8; }
.attacks .charge-btn:disabled { background: #333; color: #555; }

/* LOG */
#log-container { width: 100%; height: 140px; margin-top: 20px; }
#log { width: 100%; height: 100%; border: 2px solid #0f3460; padding: 12px; overflow-y: auto; background-color: rgba(5, 5, 10, 0.9); border-radius: 10px; box-sizing: border-box; box-shadow: inset 0 0 15px rgba(0,0,0,0.8); }
.log-entry { margin: 5px 0; font-size: 0.85rem; border-bottom: 1px solid #111; padding-bottom: 3px; }
.highlight { color: #e94560; font-weight: bold; }
.status-effect { color: #f39c12; font-weight: bold; }
.success { color: #2ecc71; font-weight: bold; }
.crit { color: #e74c3c; font-weight: bold; font-style: italic; }
.heal { color: #2ecc71; font-weight: bold; }

.card.status-shield { border-color: #3498db !important; box-shadow: 0 0 20px rgba(52, 152, 219, 0.6) !important; }
.card.status-burn { animation: burnPulse 1.5s infinite alternate !important; }
.card.status-para { filter: grayscale(60%) brightness(0.7); }

/* ANIMÁCIÓK */
.anim-attack { animation: lungePC 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards; z-index: 10 !important; }
.anim-damage { animation: shakePC 0.4s ease-in-out; border-color: #ff4757 !important; box-shadow: 0 0 25px #ff4757 !important; }
.anim-heal { animation: healPulse 0.6s ease-out; }
.anim-charge { animation: chargePulse 0.6s ease-out; }
.screen-shake { animation: globalShake 0.3s cubic-bezier(.36,.07,.19,.97) both; }

@keyframes lungePC { 0% { transform: scale(1); } 50% { transform: scale(1.05) translateY(-10px); } 100% { transform: scale(1); } }
@keyframes shakePC { 0%, 100% { transform: translateX(0); } 20%, 60% { transform: translateX(-6px); } 40%, 80% { transform: translateX(6px); } }
@keyframes healPulse { 50% { box-shadow: 0 0 30px rgba(46, 204, 113, 0.8); border-color: #2ecc71 !important; } }
@keyframes chargePulse { 50% { box-shadow: 0 0 30px rgba(243, 156, 18, 0.8); border-color: #f39c12 !important; } }
@keyframes burnPulse { from { border-color: #e74c3c; box-shadow: 0 0 10px rgba(231, 76, 60, 0.4); } to { border-color: #e67e22; box-shadow: 0 0 25px rgba(230, 126, 34, 0.7); } }
@keyframes globalShake { 0%, 100% { transform: translate(0, 0); } 20% { transform: translate(-8px, 5px); } 40% { transform: translate(8px, -5px); } 60% { transform: translate(-5px, -3px); } 80% { transform: translate(5px, 3px); } }

#victory-screen { display: none; position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: rgba(5, 5, 15, 0.95); z-index: 100; flex-direction: column; align-items: center; justify-content: center; border-radius: 16px; animation: fadeIn 0.5s ease-in-out; }
.victory-text { font-size: 2.8rem; color: #f1c40f; text-align: center; margin-bottom: 30px; text-shadow: 0 0 20px #e67e22; animation: pulse 1s infinite alternate; }
#victory-screen button { padding: 15px 40px; background-color: #2ecc71; color: white; border: none; border-radius: 8px; font-size: 1.3rem; font-weight: bold; cursor: pointer; box-shadow: 0 5px 20px rgba(46,204,113,0.4); transition: transform 0.2s; }
#victory-screen button:hover { transform: scale(1.05); background-color: #27ae60; }

@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
@keyframes pulse { from { transform: scale(1); } to { transform: scale(1.03); } }